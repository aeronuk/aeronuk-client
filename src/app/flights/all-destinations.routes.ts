import { Routes } from '@angular/router';
import { AllDestinationsComponent } from './all-destinations.component';

export const ALL_DESTINATIONS_ROUTES: Routes = [
  { path: '', component: AllDestinationsComponent },
];
